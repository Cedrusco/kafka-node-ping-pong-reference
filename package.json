{
  "name": "kafka",
  "author": "Matt Johnson (/src)",
  "version": "1.0.0",
  "description": "Test for ping pong in JavaScript",
  "main": "index.js",
  "scripts": {
    "start": "./clearAndCreateTopics.sh && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "kafka-node": "^4.0.2"
  }
}
